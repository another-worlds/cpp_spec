#include <iostream>
#include <map>
#include <vector>
using namespace std;
vector<vector<string> > buses;

int main() {
    int q;
    cin >> q;

    for (int i = 0; i < q; i++)
    {
        string command;
        cin >> command;
        if(command == "NEW_BUS") {
            
        }
    }
    

    return 0;
}