#include <iostream>
using namespace std;

bool IsPalindrom(string str) {
    if(str.size() == 0) { 
        return true;
    }
    else {
        for(int i = 0; i < str.size() / 2; i++) {
            
        }
    }
}

int main() {
    int n, res = 1;
    cin >> n;

    cout << IsPalindrom(n);


    return 0;
}
